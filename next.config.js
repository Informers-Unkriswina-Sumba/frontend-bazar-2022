/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'localhost', 'backend.unwinapreneur.com'],
  },
}

module.exports = nextConfig